// VERFÄLSCHUNGSPROBE — Completion-Regel 7. Diese Datei ist ABSICHTLICH typfalsch
// und darf NIEMALS auf main landen. Sie existiert nur, um zu belegen, dass das
// Pflichttor "TypeScript (tsc --noEmit, src + tests)" einen PR wirklich blockiert.
// Der PR dazu wird geschlossen, nicht gemergt.
export const gateProbe: number = "das ist kein number";
