# Test Log

E2E test results from Playwright runs. One row per run — failures listed inline.
Rows older than 14 days are deleted.

| # | Date | Time | Trigger | Commit | Total | Passed | Failed | Duration | Failures |
|---|------|------|---------|--------|-------|--------|--------|----------|----------|
| 27 | 2026-06-17 | — | session-end | baa3025 | 137 | 133 | 0 | 42.0s | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated). +34 content-surface tests (TC-CNT-001..031) for the ClearPath product-surface Durchstich (/glossar + /products + /products/clearpath); incl. TC-CNT-007/024 "Wie dieser Text entstand" note gate. PR #40 CI green on baa3025. |
| 28 | 2026-06-17 | — | ad-hoc | 07cd4a2 | 168 | 162 | 0 | 1.9m | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated; CI-parity retries=2). Products tree rebuild: 4 sub-portals + 8 /products/[slug] skeletons + config-driven Nav; TC-NAV-004 + TC-CNT-010 + TC-CNT-030/031 NEW_PAGES updated. 2 flaky recovered on retry (TC-NAV-006/007 nav-scroll on /impressum+/datenschutz — next-dev-under-load timing on untouched code; main baseline + standalone both green). |
| 29 | 2026-06-17 | — | ad-hoc | d0672a8 | 164 | 160 | 0 | 1.1m | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated; CI-parity). SkillCard rebuild on /products/skills — rich OSS cards replace the ProductCard grid; accessibility (no heading skip, cards @h2) + content-surface + responsive @393px all green. |
| 30 | 2026-06-17 | — | ad-hoc | 72cfb27 | 164 | 160 | 0 | 53.1s | — (4 skipped intentional; CI-parity). 5th Skills card (Restaurant-Menüpflege reference) + repo-optional SkillCard. NOTE: a prior run falsely reported 87 fail — a zombie reuse-existing dev server on :3000 (degraded under parallel-session load); killed, clean re-run = 160 pass. |
| 31 | 2026-06-17 | — | ad-hoc | eac8215 | 164 | 160 | 0 | 2.0m | — (4 skipped intentional; CI-parity). Skills cards widened to 1200px + grid md→lg (tablet-portrait single-column, landscape/desktop two-column). Responsive @393/414/768px green. |
| 32 | 2026-06-17 | — | ad-hoc | 2d005ae | 164 | 160 | 0 | 57.5s | — (4 skipped intentional; CI-parity). Nav breakpoint md→lg (hamburger until 1024px) — fixes desktop-row crowding at tablet-portrait 768px. TC-RES nav threshold updated 768→1024; desktop-row covered via navigation.spec @1280px. |
| 33 | 2026-06-17 | — | ad-hoc | 1bfd6d0 | 164 | 160 | 0 | 58.5s | — (4 skipped intentional; CI-parity). Bespoke /products/obsidian-vault-autopilot SEO/GEO page (SkillCard h1 hero + README content + mini-FAQ + SoftwareApplication/FAQPage JSON-LD); now indexable (noindex removed, hasOwnPage). TC-CNT-030/031 sweep green (single H1, 2 JSON-LD, canonical, no overflow @393). |
| 34 | 2026-06-17 | — | session-end | d22dce4 | 164 | 160 | 0 | 1.3m | — (4 skipped intentional; CI-parity). Session-end run on the shipped Skills-rebuild + OVA detail page state. All green. |
| 35 | 2026-06-18 | — | ad-hoc | acaef39 | 170 | 166 | 0 | 49.9s | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated + @external; CI-parity). NEW Cmd+K site search (+7 TC-SRCH-001..007; search-only run 7/7 incl. @external new-tab). Plus 12 search-index unit checks pass via `npm run test:search:unit` (coverage invariant). Lint + build clean, `/api/search-index` force-static prerendered. No Nav-button regression. |
| 36 | 2026-06-18 | — | ad-hoc | 1de6b56 | 84 | 84 | 0 | 28.4s | — (partial: content-surface + accessibility specs). Skills on-page overview table (+3 TC-CNT-032/033/034 — renders 5 rows, bookmarks target existing anchors, click scrolls card near top). TC-CNT-031 no-overflow@393 for /products/skills green, no heading skip. Lint + build clean. |
| 37 | 2026-06-18 | — | ad-hoc | 7194ed5 | 180 | 176 | 0 | 1.2m | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated + @external; CI-parity). /products Top-3 teaser reframe (+2 TC-CNT-014/015 — portal shows only featured per category + a "mehr" tile; sub-portal keeps full list). NOTE: a first run falsely reported 23 fail — same zombie reuse-existing dev server on :3000 as row 30 (stale code pre-edit); killed PID 3900, clean re-run = 176 pass. Lint + build + search:unit (13) clean. |
| 38 | 2026-06-18 | — | ad-hoc | 9386381 | 180 | 175 | 1 | 1.8m | TC-NAV-006 (nav-scroll /impressum → #services) — documented next-dev-under-load timing flake on untouched code; green isolated (5.8s, --workers=1), CI retries=2 recovers. Homepage flagship band: 3-bullet list folded into the sub-line ("… Flagships, MMPs, Skills (& Websites)."). No test asserts on band copy. Lint + build clean. |
| 39 | 2026-06-19 | — | session-end | 55e9815 | 181 | 177 | 0 | 1.2m | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated + @external; CI-parity). NEW TC-CNT-016 (website cards labelled "Website", not "Extern" — PR #50). Run on main after #50 (label fix) + #51 (website case-study spec) merged. Lint clean. |
| 40 | 2026-06-19 | — | ad-hoc | 50d3b42 | 184 | 180 | 0 | 1.4m | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated + @external; CI-parity). NEW snakeoil-check preview MMP detail page (+TC-CNT-035/036/037 — single H1, definition lead, 5 MMP axis headings, preview SoftwareApplication w/o url/offers per AD-19). content-surface 75/75. Lint + build clean (route ○ static, no [slug] collision; ClearPath narrowing regression-free). |
| 41 | 2026-06-19 | — | ad-hoc | f3d6738 | 187 | 183 | 0 | 56.5s | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated + @external; CI-parity). NEW Phonesis Voicebank preview MMP detail page (+TC-CNT-038/039/040 — single H1, definition lead, 6 MMP axis headings incl. "Datenschutz & Ethik", preview SoftwareApplication w/o url/offers per AD-19). Pattern-A copy of snakeoil (2nd preview wrapper). Lint + build clean (route ○ static, no [slug] collision). |
| 42 | 2026-06-19 | — | ad-hoc | 7fde577 | 190 | 186 | 0 | 55.8s | — (4 skipped intentional: ANALYTICS_READ_TOKEN-gated + @external; CI-parity). NEW prod-or-pretend preview MMP detail page (+TC-CNT-041/042/043 — single H1, definition lead, 5 MMP axis headings, preview SoftwareApplication w/o url/offers per AD-19). 3rd preview wrapper → rule-of-three: extracted shared `PreviewProductPage` component (refactor 90c6f15; snakeoil+phonesis migrated render-identical, TC-CNT-035..040 green). content-surface 81/81, search:unit 13 (31 docs). Lint + build clean (route ○ static, no [slug] collision; [slug] now owns only 3 remaining skeletons). |
| 43 | 2026-06-20 | — | session-end | be01916 | 190 | 186 | 0 | 1.4m | — (4 @external skipped). Session close on main, after the `applicationCategory→BusinessApplication` follow-up (be01916, Founder-approved) was cherry-picked onto main post the #55 merge-race; main CI E2E green on be01916. First session-end run flaked on TC-NAV-007/008 (next-dev-under-load timing on /datenschutz+/impressum, untouched code) → re-run clean 186/186; navigation isolated 10/10 (13.8s). |
| 44 | 2026-06-20 | — | ad-hoc | 84bf173 | 193 | 189 | 0 | 2.3m | — (4 @external skipped). NEW local-seo-hub preview MMP detail page (+TC-CNT-044/045/046 — single H1, definition lead, 5 MMP axis headings, preview SoftwareApplication w/o url/offers per AD-19). 4th preview wrapper on the shared `PreviewProductPage`; copy grounded in mmp-local-seo-performance-hub docs (Visibility Score 0-100, rankings/reviews/citations, SMBs + agencies). content-surface 84/84, search:unit 13 (31 docs). Lint + build clean (route ○ static, no [slug] collision). |
| 45 | 2026-06-20 | — | ad-hoc | d82b62e | 196 | 192 | 0 | 1.9m | — (4 @external skipped). NEW ai-phrase-check skill detail page (2nd skill detail page after OVA; +TC-CNT-047/048/049 — single H1, citable lead, 6 sections, SoftwareApplication w/ real GitHub url + free MIT Offer + FAQPage). Indexable: `noindex` dropped + `hasOwnPage` → enters sitemap (grep-confirmed). Honest deviation: no fabricated install command (not in marketplace) → "Verfügbarkeit" section. content-surface 87/87, search:unit 13 (31 docs). Lint + build clean (route ○ static). |
| 46 | 2026-06-20 | — | session-end | 433d680 | 196 | 192 | 0 | 3.4m | — (4 @external skipped). Session-end run on merged main (PR #56 squash 433d680: local-seo-hub MMP-preview + ai-phrase-check indexable skill detail). All green, no regressions; both pages R2-verified live 200 on prod (ai-phrase-check schema + sitemap confirmed). |
| 47 | 2026-06-20 | — | ad-hoc | e7206ec | 196 | 192 | 0 | 1.1m | — (4 @external skipped). v1 breadcrumb nav on branch `linus/breadcrumb-nav-v1`: shared `<Breadcrumbs>` (4-level trail + BreadcrumbList JSON-LD, 0→1 per indexable product page) replaces 7 hand-rolled blocks; A/B bottom nav via `<ProductDetailNav>` (Skills=Durchblättern prev/next, rest=Flach). Top breadcrumb dropped from the noindex skeletons (`[slug]` + `PreviewProductPage`). `accessibility.spec` green (distinct nav aria-labels → axe landmark-unique clean). Lint + build 30/30 clean. Lighthouse (Skills detail, two navs): Perf 100 / A11y 96 / BP 100 / SEO 100. |
| 48 | 2026-06-20 | — | ad-hoc | f974a04 | 196 | 192 | 0 | 47.5s | — (4 @external skipped). breadcrumb nav v2 (Founder decision on the #58 preview page): chevron separator (`/`→`›`) + ONE inline end-of-page nav (`← prev · Alle <Kategorie> · next →`) replacing the tiles/flat split; top breadcrumb re-added to the thin noindex skeletons (`[slug]` + `PreviewProductPage`) → anchored on ALL templates. Lint + build 30/30 clean. Lighthouse (detail, two navs): Perf 100 / A11y 96 / BP 100 / SEO 100. |
| 49 | 2026-06-21 | — | ad-hoc | 51077ca | 192 | 188 | 0 | 1.1m | — (4 @external skipped). PR1 products-card refinement on branch `linus/products-cards-quickwins`: SkillCard GitHub button → header top-right (footer keeps license-left + "Mehr erfahren →"-right, Image #1); ProductCard opt-in rich mode (`description`+`repoUrl`, `<div>` not whole-card-link) → MMP sub-portal gets ≈3-line descriptions + GitHub buttons (PRIVATE repos, Founder-chosen). New `src/lib/mmp-cards.ts` (server-only). `content-surface` TC-CNT-011/014/015 + `navigation` green (MMP cards still link `/products/<slug>` via title + CTA). Lint + build 30 routes clean. search:unit 13 (31 docs). |
| 50 | 2026-06-21 | — | ad-hoc | d930ba0 | 199 | 189 | 0 | 4.3m | All full-run reds were load-timeout flakes, 0 real (each re-passed in isolation: PR2 TC-NAV-004 hover; M1 batch TC-CAL-002/TC-CNT-002-004 bestaetigungsfehler/TC-NAV-007 → 10/10 isolated in 16.8s). Branch `linus/skill-detail-pages` = PR2 (3 skill detail pages via new `SkillDetailPage` template, rule-of-three; `detailHrefFor` keys on `hasOwnPage` not `noindex`; TC-CNT-033 anchors=0 / TC-CNT-034 rewritten / restaurant added to NEW_PAGES) + Msg-1 (≈3-line card descriptions on /products teaser + Websites/Flagships sub-portals, new `card-descriptions.ts`). R2: sitemap social+imap IN / restaurant OUT; robots noindex only restaurant; schema types correct. Lint + build 30 routes. search:unit 13 (31 docs). |
| 51 | 2026-06-21 | — | ad-hoc | 6299b8f | 210 | 206 | 0 | 1.3m | — (4 @external skipped); flake-free full run. Branch `linus/website-case-studies` (PR #63) = website case-study detail pages for all 4 sites (`/products/websites/[slug]`): new `websites` content collection (4× md, 7 content axes; sub-agent-drafted per source repo + editor-passed), loader `content/websites.ts`, `CreativeWork` schema, route nested under the sub-portal, `portfolio.ts` `caseStudySlug`+`liveUrl` (Model A: `isExternal` stays true → flat `/products/<slug>` still 404s), `ProductCard` "Beides" mode, sitemap adds the 4 case-study routes. 11 new TC-CNT-050..054 (incl. flat-path-404 + sitemap-not-external routing guards). Lint clean, build 34 routes (all 4 prerendered). |
| 52 | 2026-06-21 | — | ad-hoc | 6299b8f+ | 7+13 | 7+13 | 0 | 10.3s | search-index consistency follow-up (advisor done-check): Cmd+K website entries now resolve to the INTERNAL case study (`/products/websites/<slug>`), not the external domain — on-site search opens the case study, live site one click further. `index-data.ts` uses `caseStudySlug ? internal : href`; `search:unit` 13/13 (31 docs, 3 assertions updated); `search.spec` 7/7 (TC-SRCH-007 flipped external-new-tab → internal-nav, `@external` tag dropped → now runs in CI). |
| 53 | 2026-06-21 | — | ad-hoc | linus/product-faqs | 221 | 217 | 0 | 58.4s | — (4 @external skipped); flake-free full run. Branch `linus/product-faqs` (FAQ on all product detail pages): new shared `ProductFaq` (visible details/summary section + FAQPage JSON-LD gated on `!noindex` via portfolio); central `product-faqs.ts` (11 pages, AI-draft → Founder-edit); `SkillDetailPage` delegates FAQ to `ProductFaq` (social/imap unchanged, restaurant gains noindex-gated FAQ); wired omnopsis/clearpath + `PreviewProductPage` (×4 MMP) + `websites/[slug]` (×4 case study). 11 new tests: TC-CNT-055 (6 indexable — FAQ + FAQPage) + TC-CNT-056 (5 noindex — FAQ, no schema). Lint clean; Lighthouse omnopsis Desktop/5G/4G all hard gates pass (A11y 96 / BP 100 / SEO 100). |
| 54 | 2026-06-22 | — | ad-hoc | linus/clickable-cards | 227 | 223 | 0 | 1.3m | — (4 @external skipped). Branch `linus/clickable-cards`: (1) all portal/sub-portal cards fully clickable — a stretched `::after` overlay on the "Mehr erfahren" link makes the whole rich/Beides/Skill `<div>` card navigate to the detail page; the GitHub/Website buttons + title link carry `relative z-10` to stay independently clickable; whole-card hover affordance. +4 tests: TC-CNT-057 (card-body click → detail page on mmps/websites/skills) + TC-CNT-058 (GitHub button stays external, not detail). (2) Website tier shows lowercase domain names (ristorante-goldoni.de / oakwoodgolfclub.de / rauhut.com) on portal cards + breadcrumb + meta title + CreativeWork schema; `website-case-study` test data updated. Lint clean; Lighthouse /products/mmps Desktop/5G/4G all hard gates pass (A11y 96 / BP 100 / SEO 100). |
| 55 | 2026-06-22 | — | session-end | linus/domain-subtitle-fix | 227 | 223 | 0 | 1.6m | — (4 @external skipped). Session-end. Branch `linus/domain-subtitle-fix`: (1) RE-SHIPS the website domain-subtitle link — commit `2ea75c4` was MISSED by #65's squash merge (pushed after the squash point), so the requested subtitle never reached main/prod; cherry-picked here as `2ac0a1d`. The lowercase domain under the H1 on each `/products/websites/[slug]` links to the live site (all 4 incl. neckarshore.ai → https://neckarshore.ai). (2) New manual test case TC-MAN-FAQ-001 (Google Rich-Results on the 6 FAQPage URLs). Lint clean. |
| 56 | 2026-06-22 | — | ad-hoc | linus/mmp-card-github-top-right | 227 | 223 | 0 | 1.5m | — (4 @external skipped). MMP rich cards restructured to mirror the SkillCard layout (Founder): GitHub button → top-right, "Mehr erfahren →" → bottom-right (was a footer row with Mehr-erfahren-left + GitHub-right); title moved to its own row below the badge/GitHub row. Stretched-link clickability + z-10 GitHub independence preserved — `elementFromPoint`-verified for snakeoil-check (badge) + clearpath (no badge, live). No test changes needed (TC-CNT-057/058 + a11y + responsive all green). Lint clean. |
| 57 | 2026-06-22 | — | ad-hoc | linus/unify-card-layout | 228 | 224 | 0 | 53.1s | — (4 @external skipped). UNIFIED 4-corner card principle across MMP/Skill/Website/Flagship (Founder): title top-left, GitHub/Website↗ top-right, status pill bottom-left, "Mehr erfahren →" bottom-right. New shared `StatusPill` component + `statusPillLabel(item)` helper — honest per item (omnopsis "In Entwicklung" not "Live" despite status:"live"). ProductCard rewritten to ONE unified render (all modes); SkillCard restructured (icon+title TL, Beta/Referenz-Beispiel pill + MIT text BL). +TC-CNT-059 (status pill on every card type); TC-CNT-016 updated (Live pill + Website link). `elementFromPoint`-verified all 4 corners across MMP/Website/Skill (snakeoil/clearpath/neckarshore/oakwood/OVA/restaurant). New docs/card-layout.md + Obsidian-Nexus view. Lint clean; Lighthouse /products/skills Desktop/5G/4G all hard gates pass (A11y 96). |
| 58 | 2026-06-22 | — | ad-hoc | linus/products-indexing-geo | 230 | 211 | 15 | 9.0m | ⚠️ 15 FAILED were env-flake on UNTOUCHED pages (TC-A11Y-006..009 /impressum, TC-CAL-001/002, TC-CNT-002/030/031 glossar+/products+clearpath, TC-NAV-006/007/009) under a degraded next-dev server (9.0m vs ~1min normal — same class as #28/#30). Targeted clean re-run of all 15 = **52/52 passed (36.4s)** → recovered, NOT a regression. All changed-surface tests GREEN in the main run: TC-CNT-055/056 (4 MMP previews → FAQ_INDEXABLE), TC-CNT-036/039/042/045 (question-H2s), TC-CNT-037/040/043/046 (preview schema no url/offers), TC-SEO-021/022 (robots OAI-SearchBot + llms.txt products). Scope-A indexing+GEO pass. Lint + build green; search:unit 13/13. |
| 59 | 2026-06-22 | — | ad-hoc | linus/products-detail-cta-table | 240 | 236 | 0 | 2.2m | — (4 @external skipped); flake-free full run. Branch `linus/products-detail-cta-table` = 3 Founder detail-page requests: (1) end-of-page "Kennenlerntermin" CTA mobile-only via `ProductDetailNav.hideCtaOnDesktop` (`lg:hidden`) on omnopsis + websites/[slug]; (2) skill detail pages drop the "Auf GitHub ansehen" link (repo URL stays in JSON-LD); (3) Technik & Architektur prose → "Baustein \| Detail" table on all 4 website case studies + editorial `.prose-content table` CSS. +6 new tests: TC-CNT-060 (no GitHub CTA ×4 skill pages), TC-CNT-061 (CTA hidden@1280 / visible@393 ×2), TC-CNT-062 (Technik table ×4 sites). Lint + tsc(src) clean; renderMarkdown table-render verified. |
| 60 | 2026-06-22 | — | ad-hoc | linus/detail-page-review-fixes | 241 | 235 | 2 | 2.0m | ⚠️ TC-NAV-006/007 (nav-scroll /impressum+/datenschutz → #services) — documented next-dev-under-load timing flake on UNTOUCHED pages (same class as #58); isolated re-run **2/2 passed (8.2s)**, CI retries=2 recovers. All changed-surface GREEN: TC-CNT-063 (skills overview Code/Status cols hidden@393, shown@1280) + TC-CNT-050/051/062 ×4 (website Laufende-Pflege+Ausblick de-jargon — H2 axes intact, Technik table intact). 2 Founder review-fixes: skills overview table flatter on phones + plain-language rewrite of the two jargon-heavy axes on all 4 case studies. 4 @external skipped. Lint clean. |
| 61 | 2026-06-23 | — | ad-hoc | linus/seo-lastmod | 241 | 229 | 8 | 5.7m | ⚠️ 8 FAILED were env-flake on UNTOUCHED surfaces (TC-STAT-009-6, TC-CAL-001/002, TC-NAV-006/007/008/009, TC-RES-004) under a cold-compile next-dev full run — same class as #58/#60. Targeted clean re-run (4 specs, fresh :3000) = **29/29 passed (18.2s)** → recovered, NOT a regression. Changed-surface GREEN incl. TC-CNT-054 (sitemap carries the case-study routes) + TC-SEO-009 (sitemap accessible). Feature = stable sitemap `lastmod` (`SITE_UPDATED` const kills the per-build `new Date()` false-freshness signal; SEO/GEO audit 2026-06-23). +2 unit tests TC-SEO-023/024 (`npm run test:unit` 12/12); search:unit 13/13; lint + build clean; built sitemap.xml = all 26 URLs `2026-06-22`. |
| 62 | 2026-06-23 | — | session-end | linus/glossar-to-clearpath | 218 | 214 | 0 | 1.7m | — (4 skipped intentional: @external + ANALYTICS_READ_TOKEN-gated); flake-free full run. Retired the standalone `/glossar` surface → consolidated the cognitive biases into a lean table on `/products/clearpath` (Founder decision: bias content is ClearPath's domain, not neckarshore's). Removed glossar route/loader/DefinedTerm-schema + 3 content `.md`; **308-redirects** `/glossar` + `/glossar/:slug*` → `/products/clearpath`; de-wired sitemap (26→22 URLs), search-index + `SearchType`, Footer/homepage/SearchOverlay, prose refs. New bias table = 3 secured definitions + **R2-verified** de.wikipedia links (caught a 404 on Überlebenden-Verzerrung → uses `Survivorship_Bias`). Tests: −19 glossar TC-CNT, +TC-CNT-064/065 (redirects), rewrote TC-CNT-022 (bias table) + TC-SRCH-001 (page-nav); `search:unit` 12/12; `test:unit` 12/12; lint + build clean. |
| 63 | 2026-06-24 | — | ad-hoc | linus/products-geo-discovery | 215 | 211 | 0 | 6.1m | — (4 @external skipped); flake-free full run. SEO/GEO products-surface audit follow-ups (2 commits, 1 PR #74): **(1) ship-safe AI-discovery config** — (G1/G6) `llms.txt` Websites section gains the 4 case-study URLs (`/products/websites/{neckarshore,ristorante-goldoni,oakwood-golf-club,rauhut}`) + per-project one-liners; (G5) `robots.txt` explicit `Bingbot` Allow. +TC-SEO-025/026 (**ID-collision avoided** — 023/024 are the sitemap unit tests). **(2) C1 Omnopsis milestone honesty (Founder-decided)** — hardcoded `MVP Q2 2026` (stale on 2026-06-30) decoupled to `Launch geplant Q3 2026` in all 3 user-facing places (pill, Conceived/Born block, "Wann verfügbar?" FAQ answer **+ its FAQPage JSON-LD**) + 2 code comments. +TC-CNT-066 (`page.content()` guard: no `Q2 2026` on /products/omnopsis). Lint + build clean; `search:unit` 12/12. Content-depth + copy-judgment findings routed to MASCHIN (FOR MASCHIN in session report). |
| 64 | 2026-06-25 | — | ad-hoc | linus/dpo-consent-neckarshore | 221 | 217 | 0 | 1.4m | — (4 @external skipped); flake-free clean run. **First run discarded** — Playwright `reuseExistingServer` reused a foreign grafowerk dev server on :3000 (parallel session), so neckarshore tests hit grafowerk (TC-SEO-013 resolved og:title="Grafowerk"); re-ran on dedicated :3100 → valid. DPO Blocker 1 (Gutachten §2): removed CookieBanner (component + page.tsx import/usage, F1); datenschutz §4 Webanalyse (F2/F4 — transient-IP + SHA-256 visitor-hash + Vercel geo-header disclosure), §8 Cookies→"Speicherung im Browser" (F3, kills the internal §4↔§8 contradiction), §3 Vercel geo-header. Verbatim DPO Pflichttext. Build + lint clean. Pending dr-sommer sign-off (Two-Stage Completion). |
| 65 | 2026-06-25 | — | ad-hoc | linus/homepage-oss-remove-portfolio-reorder | 217 | 213 | 0 | 1.6m | — (4 skipped); **first run discarded** — same foreign grafowerk :3000 `reuseExistingServer` trap as #64; clean re-run on dedicated :3100 = valid. Founder homepage changes: (1) removed OPEN-SOURCE section (`#open-source` "Tools die wir selbst benutzen", 2 OSS cards — covered under /products) + 3 now-unused icon imports (Terminal/Layers/ExternalLink); (2) moved "Unser Portfolio" (`#omnopsis` Flagship + StatsGrid) above the "Warum Nearshore?" band (portfolio leads); (3) kept the bordeaux announcement strip (30-day Wiedervorlage 2026-07-25) but repointed its link `/#open-source` → `/products/obsidian-vault-autopilot` (anchor deleted). Regression-critical specs GREEN: TC-SMK-002 (key sections present), navigation anchors, SEO, a11y heading-hierarchy, responsive. Lint + build clean. |
| 66 | 2026-06-25 | — | ad-hoc | linus/homepage-faq-merge | 217 | 206 | 7 | 5.0m | ⚠️ 7 full-run FAILED were next-dev-under-load timing flakes on UNTOUCHED surfaces (TC-LNK-001 + TC-NAV-004/005/007/008/009 …) — same class as #58/#60/#61; isolated re-run navigation+links on dedicated :3100 = **19/19 passed (23.4s)**, CI retries=2 recovers. Changed surface (FAQ) GREEN in the full run (content-surface/seo/a11y not among the reds). Founder homepage change: MERGED the two bottom Q&A sections into ONE "Häufige Fragen" `<details>` accordion — combined `faqItems`(5)+`snippetItems`(3) → 7 items (BYOLLM de-duped, long-form GEO answers kept inside the accordion → still in SSR HTML for crawlers), deleted the standalone "Knowledge Base" long-form section, single FAQPage JSON-LD (`faqItems` only). Same pattern as `ProductFaq`. Lint + build clean. |
| 67 | 2026-06-28 | — | ad-hoc | linus/markdown-export | 229 | 225 | 0 | 1.3m | — (4 @external skipped); flake-free full run. NEW feature: "Als Markdown" export on product detail pages. Server-side **source**-markdown export (clean `.md` round-trips into `src/content/products/*.md`): `/api/export?path=` → frontmatter header + raw body + data-driven FAQ as `Content-Disposition: attachment`, **download-only** → `ExportButton` is a plain `<a download>` (zero client JS). +6 E2E TC-EXP-001..006 (button on bespoke clearpath + PreviewProductPage; endpoint 200/text-markdown/attachment; body has frontmatter+body+FAQ; 404 non-exportable; 400 bad path) + 8 unit TC-EXP-U01..U08 (`test:unit` 20/20). Prod build+`next start` verified **file-tracing** (`outputFileTracingIncludes` content dir): 200 clearpath+snakeoil, 404 datenschutz+traversal, 400 bad-path. Lint + build clean. |
| 68 | 2026-06-28 | — | ad-hoc | linus/markdown-export | 230 | 226 | 0 | 2.3m | — (4 @external skipped); **clean run on dedicated :3101**. First full run on default :3000 showed 12 "failures" (TC-EXP-003 + TC-SRCH-006/007 + TC-SMK-005 + TC-CNT-031/057/061/063) = ALL the `reuseExistingServer` stale-server trap (#64/#65 class): a pre-existing `next-server` on :3000 predated the new `product-sections.ts`, so its export lacked the biases table AND the dev server was degraded; re-run on fresh :3101 = 226/226. Feature increment (Founder request): product-specific **TABLES** now in the export — generic `tableToMarkdown()` + slug-keyed `extraSectionsForSlug()` registry (ClearPath "Die wichtigsten Denkfehler" from `CLEARPATH_BIASES` → GFM table, ordered before FAQ; one entry per future table). +4 unit TC-EXP-U09..U12 (`test:unit` 24/24) + TC-EXP-003 table assertions. Prod build+`next start` curl confirms the rendered table. Lint + build clean. No Lighthouse delta (page rendering unchanged — the table was already on-page; only the export gained it). |
| 69 | 2026-06-28 | — | session-end | 3c209eb | 230 | 226 | 0 | 2.7m | — (4 @external skipped); **clean run on dedicated :3101**. Session = social-scrapers set to PRIVATE (Founder/legal): dropped all OSS/MIT/free claims (text + JSON-LD), `visibility` public→private, badge Beta→"In Entwicklung", GitHub button kept. First full run on default :3000 showed the SAME 12 "failures" as #68 = the `reuseExistingServer` stale-server trap (a pre-existing `next-server` on :3000 served stale code) — STASH-verified identical on a clean tree, so NOT a regression; clean :3101 re-run = 226/226. social-scrapers content tests (TC-CNT-030/031/055/060) green against fresh source. Lint + prod build clean. No Lighthouse delta (text/schema-only change, no render-path delta). |
| 70 | 2026-06-28 | — | ad-hoc | linus/md-export-filename | 230 | 226 | 0 | 1.3m | — (4 @external skipped); **clean run on dedicated :3101**. Export download filename now **site-prefixed**: `neckarshore.ai - clearpath.md` instead of `clearpath.md` (Founder convention 2026-06-28 — self-identifying when many sites' exports share one Obsidian vault / LLM context). One-line builder change (`src/lib/export/builders/product.ts`: filename `${SITE_NAME} - ${slug}.md`; `SITE_NAME` already derived from `SITE_URL`), propagating to BOTH the HTTP `Content-Disposition` and the `<a download>` browser name (single source). **The dot in `.ai` does NOT mangle the download** — TC-EXP-007 (real browser download-click) confirms `suggestedFilename() === "neckarshore.ai - clearpath.md"`, so the Founder's hyphen-fallback (`neckarshore-ai - …`) is NOT needed. Tests: updated TC-EXP-002/007 (e2e) + TC-EXP-U05/U07 (unit), +TC-EXP-U13 (builder filename leads with host); `test:unit` 25/25; `search:unit` 12/12. Lint + build clean. |
| 71 | 2026-06-28 | — | ad-hoc | b92e3a5 | 253 | 249 | 0 | 1.6m | — (4 @external skipped); full run, flake-free. SEO/GEO audit Gate-3 mechanical fixes (branch `linus/seo-audit-mechanical-fixes`): +23 e2e TC-SEO-027..030 written RED then GREEN — TC-SEO-027 (20 paths: every /products surface `<title>` ≤70 chars; trimmed 7 from 74-85), TC-SEO-028 (/products emits BreadcrumbList), TC-SEO-029 (social-scrapers SoftwareApplication has no `url`), TC-SEO-030 (neckarshore case-study title de-double-branded). No regressions across nav/links/content-surface/website-case-study. Lint + prod build clean. |
| 72 | 2026-06-28 | — | session-end | b25daa6 | 253 | 249 | 0 | 3.2m | — (4 @external skipped); full run on merged main (post #81 squash). Confirms #71 result holds on origin/main after the Founder merge — identical code, 0 regressions. Session-end gate for the SEO-audit-mechanical-fixes session. |
| 73 | 2026-06-28 | — | ad-hoc | bbb607a | 262 | 258 | 0 | 2.4m | — (4 @external skipped); full run on branch `linus/entity-webpage-id`. Per-route WebPage entity (L-NECK-ENTITY-WEBPAGE-ID): +9 e2e TC-SEO-031..035 written RED (8 fail / 1 pass — homepage already had the correct `@id`) then GREEN. The layout's homepage-pinned WebPage (`/#webpage` on every route) is split out → each indexable route now emits exactly one self-asserting WebPage co-located with its primary entity in ONE per-route `@graph` (`<PageSchema>`), `mainEntity` wired from the entity's own `@id`. content-surface + website-case-study `@type` filters made `@graph`-aware (`ldNodes`, backward-compatible — verified green on pre-fix code before the refactor). TC-SEO-029 still green (social-scrapers no `url`). Offline JSON-LD validator: all mainEntity/isPartOf/about refs resolve, 0 dangling, across 8 routes incl. noindex restaurant (no url/offers). Lint + `next build` clean; `src/` tsc clean (pre-existing `tests/*` tsc noise unrelated). |
| 74 | 2026-06-28 | — | session-end | 5e1811b | 263 | 259 | 0 | 1.7m | — (4 @external skipped); session-end gate on merged main (PR #82 squash). +1 vs #73 = TC-SEO-036 (the advisor-surfaced co-location guard, added in the 3rd branch commit after #73's run): one `@graph` block holds the WebPage AND its `mainEntity` target with a single `@context` — guards the cross-block-split regression that the flatten-helpers (TC-SEO-034 + the offline validator) cannot see. New per-route WebPage block 10/10 (TC-SEO-031..036). 0 regressions on the merged commit; main CI (Lint+Unit+Lighthouse+E2E) all green on 5e1811b. Session-end gate for the L-NECK-ENTITY-WEBPAGE-ID session. |
| 75 | 2026-06-30 | — | ad-hoc | linus/post-audit-honesty-block | 261 | 257 | 0 | 2.3m | — (4 @external skipped); flake-free full run. L-NECK-POST-AUDIT-POLISH honesty block, **no test delta** (261 = inherited origin/main baseline post #83+#84; honesty/rename fixes add no tests). 3 fixes: (1) social-scrapers card copy `TypeScript-Lego`→`Python-Lego` (**R2 against the ground**: scraper repos verified 14 `.py`/0 `.ts` in `social-scrapers-common`, 908 `.py`/2 `.js` in `linkedin-scraper` — card contradicted reality); (2) `PreviewProductPage`→`ProductDetailPage` rename (file + default export + props iface + `previewProductMetadata`→`productDetailMetadata` across the 4 product wrappers + 1 `portfolio.ts` comment; misnomer — the component renders preview AND live since the snakeoil go-live; correctly-named `previewSoftwareApplicationSchema`/`liveSoftwareApplicationSchema` left intact); (3) repo `CLAUDE.md` CI-Health-Check: the false "No dedicated `e2e.yml` workflow … runs locally only / backlog item" claim corrected — `e2e.yml` exists and gates every PR + push to `main` via `test:e2e:ci` (`--grep-invert @external`). The `/products/*` routes (which import the renamed component) render green → rename break-free. `search:unit` 12/12, `test:unit` 25/25, ESLint clean, `src/` tsc clean (pre-existing `tests/*` tsc noise unrelated). |
| 76 | 2026-06-30 | — | ad-hoc | linus/llms-full-txt | 265 | 261 | 0 | 2.3m | — (4 @external skipped); flake-free full run. **#75 is the parallel `linus/post-audit-honesty-block` branch (PR #85, FM-6) — merged in ascending order on rebase.** +4 e2e TC-SEO-039..042 (RED→GREEN) for the NEW `/llms-full.txt` route (L-NECK-POST-AUDIT-POLISH 9a). `force-static` route (`app/llms-full.txt/route.ts`) serving the llmstxt.org "full" variant: the curated `public/llms.txt` overview + the FULL Markdown of every md-backed indexable product (clearpath / snakeoil-check / phonesis / local-seo-hub / prod-or-pretend), assembled by `buildLlmsFullText()` reusing **`allProductRoutes()` ∩ `resolveExport()`** — the page set is derived from the sitemap's indexable route set + the export resolver, so it **cannot drift** from the sitemap / `llms.txt` / the per-page Markdown download (AP-1: a new exportable product appears automatically). `exportedAt` pinned to `SITE_UPDATED` → byte-deterministic, no build-date drift (same rationale as sitemap `lastModified`). +1 discovery pointer line in `llms.txt`. **Prod build verified:** `○ /llms-full.txt` prerendered static, `.body` = 25 965 B (~6.2× the 4 206 B `llms.txt`), `content-type: text/plain; charset=utf-8`. unit 25/25, search:unit 12/12. Lint + `next build` + `src/` tsc clean. |
| 77 | 2026-06-30 | — | ad-hoc | linus/website-export-button | 290 | 286 | 0 | 2.4m | — (4 @external skipped); flake-free full run. **"Als Markdown" export button extended to the website case-study pages** (`/products/websites/<slug>`, Founder request). +2 e2e (TC-EXP-008 button+endpoint on the case-study page; TC-SEO-044 case studies now inlined in `/llms-full.txt`) +2 unit (TC-EXP-U14 `buildWebsiteMarkdown` from real `.md`+FAQ; TC-EXP-U16 two-segment route maps to the website builder, NOT swallowed by `PRODUCT_PATH`), TDD red→green. New `builders/website.ts` reads the `websites` collection (frontmatter+raw body→round-trips into `src/content/websites/<slug>.md`), adds `status`+`stack` frontmatter; `resolveExport()` gets a `WEBSITE_PATH` matcher (mutually exclusive with `PRODUCT_PATH` via the `$` anchor; `[a-z0-9-]` closes traversal). **Bonus, single-source/AP-1:** the matcher pulls the 4 case studies into `/llms-full.txt` automatically (`allProductRoutes()` ∩ `resolveExport()`) → AI-ingestable. unit 37/37, search:unit 12/12, ESLint clean, `src/` tsc clean (pre-existing `tests/*` `.ts`-import tsc noise unrelated). |
